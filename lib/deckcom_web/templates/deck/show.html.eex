<% IO.puts inspect @deck.play %>
<%= for ip <- @deck.play do %>
    <% card = Deckcom.Cards.get_card(id: ip) %>
    <img src="<%= Enum.at(card.image_uris, 0) %>" style="width: 10.5vw; height: 14.5vw;">
<% end %>